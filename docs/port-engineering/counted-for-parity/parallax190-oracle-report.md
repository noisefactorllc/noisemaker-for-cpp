# parallax190 oracle report

Generated by `parallax190_oracle_generator.mjs` from an immutable snapshot
of `noisemaker-for-cpu`. Authority: `canonicalFactory98`, factory text
`052b2b2cc1a086a57c458fd8ae49b6065391f19f55feaa0662e14f6614cde905`.

The authority argument is serialized only as `<immutable-cpu-snapshot-root>`; the
required live identity input is serialized only as
`<live-noisemaker-for-cpu-checkout>`. No host checkout path participates in
the generated document.

This package exists because typed row 190 shipped without one and was wrong.
See `../DEFECTS-FOUND.md` item 6 and `parallax190-alias-divergence.md`.

## The alias contract

```
var prevUV = rayUV;
(rayUV[0] = uv[0] + shift[0] * (t - pivot), rayUV[1] = uv[1] + shift[1] * (t - pivot), rayUV);
mix(rayUV, prevUV, w).reduce((res,el,i)=>(res[i] = el, res), rayUV);
```

`prevUV` and `rayUV` are one `PooledFloat32Array`. The refinement is
therefore `mix(x, x, w) == x` -- a no-op in the authority. The generator
asserts each of those three lines occurs exactly once, and that `prevUV` is
never rebound.

## Cases

- **full-basic** (full, 5x4) --- DOES NOT discriminate the refinement mutant -- kept deliberately, because the shipped defect was invisible at exactly this shape
- **straddle** (full, 4x5) --- the primary discriminator: every pixel enters the refinement with a nonzero w
- **straddle-tile** (tile, 4x5) --- the same march under a nonzero tileOffset -- pins the tile offset rule and the refinement together
- **zero-direction** (full, 6x3) --- the length(direction) == 0 fallback arm: v becomes vec3(0,0,1), shift is zero, the march is a fixed point
- **tile-clamped** (tile, 4x4) --- the ONLY case that reaches the dispPixels > maxDispPixels clamp arm
- **mismatched-maps** (full, 6x4) --- heightMap and inputTex at different sizes -- pins the two independent textureSize divisors apart

## Mutation ledger --- changed float32 lanes per case

| mutant | budgeted as | full-basic | straddle | straddle-tile | zero-direction | tile-clamped | mismatched-maps |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `refinement-copy-restored` | discriminator | . | 6 | 6 | . | 42 | . |
| `refinement-weight-negated` | measured invariant | . | . | . | . | . | . |
| `march-steps-halved` | discriminator | . | . | . | . | 24 | . |
| `shift-scale-halved` | discriminator | . | 24 | 24 | . | . | . |
| `luminosity-weights-swapped` | discriminator | . | 3 | 3 | . | 6 | . |
| `textureLod-becomes-texelFetch-origin` | discriminator | . | . | 60 | . | 42 | . |

`.` means bit-identical to the canonical render.

**`refinement-copy-restored` is the regression guard.** It reproduces the
emission the port shipped at row 190. Note that `full-basic` does NOT
discriminate it: the defect was invisible at that shape, which is why the
case is kept rather than dropped. The generator fails if that ever stops
being true.

`refinement-weight-negated` is budgeted as a measured **invariant**, not a
discriminator: under the alias the weight multiplies a zero delta. Its
all-identical row is the positive evidence that the refinement is inert.

## Controls

- **external-time-and-seed-extreme**: identical --- parallax declares neither uniform; runPass time/seed cannot reach it

## Comparer self-tests

- identical-is-exact: pass
- one-lane-differs-is-caught: pass
- sub-quantization-difference-is-caught: pass
- sub-quantization-difference-moves-no-byte: pass
