#!/usr/bin/env node
// LightLeak192 canonical pixel oracle. This package never participates in
// typed-kernel generation.
import crypto from 'node:crypto'
import fs from 'node:fs'
import path from 'node:path'
import { fileURLToPath, pathToFileURL } from 'node:url'

const here = path.dirname(fileURLToPath(import.meta.url))
const cppRoot = fs.realpathSync(path.resolve(here, '../../../..'))
const outputPath = path.join(here, 'lightleak192-oracles.json')
const reportPath = path.join(here, 'lightleak192-oracle-report.md')
const generatorPath = fileURLToPath(import.meta.url)
const sourceRelative = 'tools/glslcpp/corpus/a024dc3a960cc44af454abc7aebce50456c194e6/sources/filter/lightLeak/lightLeak.glsl'
const sourcePath = path.join(cppRoot, sourceRelative)
const programKey = 'filter/lightLeak:lightLeak'
const authorityNode = 'v24.7.0'
const upstreamRevisionExpected = '117a236679d1db3ab8f0e278230ece277b57564c'
const authorityCommit = '4834b0144ee0524588144a482cca0067b15f68ec'
const factoryName = 'canonicalFactory77'
const factoryBytesExpected = 7818
const factoryShaExpected = '9cf716594f8d25347737104d2ec0658276ac5a11405eb878706dc8f429c9055f'
const sourceBytesExpected = 5047
const sourceShaExpected = '61bcb2989992c109dcf73ac5b34bb4dfa7f6603b54c111a84e69b6f73a9501bb'
const f = Math.fround
function sha256(value) { return crypto.createHash('sha256').update(value).digest('hex') }
function bytes(view) { return Buffer.from(view.buffer, view.byteOffset, view.byteLength) }
function words(view) { return new Uint32Array(view.buffer, view.byteOffset, view.byteLength / 4) }
function u32Hex(value) { return `0x${(value >>> 0).toString(16).padStart(8, '0')}` }
function sidecarPath(target) { return `${target}.sha256` }
function sidecarText(target, payload) { return `${sha256(payload)}  ${path.basename(target)}\n` }
function verifySidecar(target) { const sidecar = sidecarPath(target); if (!fs.existsSync(target) || !fs.existsSync(sidecar)) throw new Error(`missing sidecar: ${target}`); const payload = fs.readFileSync(target); if (fs.readFileSync(sidecar, 'utf8') !== sidecarText(target, payload)) throw new Error(`sidecar drift: ${target}`) }
function beneath(root, candidate) { return candidate === root || candidate.startsWith(`${root}${path.sep}`) }
function rejectSymlinkLeaf(candidate, label) { const leaf = path.resolve(candidate); try { if (fs.lstatSync(leaf).isSymbolicLink()) throw new Error(`${label} must not be a symlink: ${candidate}`) } catch (error) { if (error?.code === 'ENOENT') return; throw error } }

const argv = process.argv.slice(2)
const modes = argv.filter((token) => ['--write', '--check', '--self-test'].includes(token))
if (modes.length !== 1) throw new Error('choose exactly one of --write, --check, or --self-test')
const mode = modes[0]
const cpuRootIndex = argv.indexOf('--cpu-root')
if (cpuRootIndex < 0 || cpuRootIndex + 1 >= argv.length) throw new Error('--cpu-root <immutable snapshot> is required')
for (const [index, token] of argv.entries()) if (index !== cpuRootIndex && index !== cpuRootIndex + 1 && token !== mode) throw new Error(`unexpected argument: ${token}`)
const cpuRootArgument = argv[cpuRootIndex + 1]
rejectSymlinkLeaf(cpuRootArgument, '--cpu-root')
if (!fs.existsSync(cpuRootArgument) || !fs.statSync(cpuRootArgument).isDirectory()) throw new Error(`--cpu-root is not a directory: ${cpuRootArgument}`)
const cpuRoot = fs.realpathSync(cpuRootArgument)
const liveCpuCheckout = process.env.NOISEMAKER_FOR_CPU ?? (process.env.HOME ? path.join(process.env.HOME, 'platform/noisemaker-for-cpu') : null)
if (liveCpuCheckout) rejectSymlinkLeaf(liveCpuCheckout, 'NOISEMAKER_FOR_CPU')
if (!liveCpuCheckout || !fs.existsSync(liveCpuCheckout) || !fs.statSync(liveCpuCheckout).isDirectory()) throw new Error(`live noisemaker-for-cpu checkout does not exist: ${liveCpuCheckout ?? '<unset>'}`)
const liveCpuReal = fs.realpathSync(liveCpuCheckout)
if (beneath(liveCpuReal, cpuRoot) || beneath(cpuRoot, liveCpuReal)) throw new Error('--cpu-root must be an immutable snapshot, never the live checkout')
if (beneath(cppRoot, cpuRoot)) throw new Error('--cpu-root must not live inside the C++ repository')

const expectedClosure = Object.freeze([
  ['src/csl/glsl-kernel.js', 'a684b1bc16f095c550e488d1db35b9cea9c69b761db6ad3af175110e6a2e2baa'], ['src/csl/glsl-runtime.js', 'a20421c56aa3274746f6887555445b8c7f7bb8318921fe6f75f6aa8ffe71c072'], ['src/csl/runtime.js', 'a34ac04d63bb0865081ad3964b1ff5a427664a80e35f43c1761d91b0ea8e7dee'],
  ['src/effects/adapters/bit-effects.js', '5044fc318e69deb1e03330f977af1f8a76376c69936ebf0a6d33fe350379d7c7'], ['src/effects/adapters/crt.js', 'c424c45169894e1d39eb11dc97c1835991fa9e990f3dd7c1aeefafbfe9f3a5cc'], ['src/effects/adapters/f32-color.js', 'b0d2562969029701f44b049dbfa17fc7a13f97758c3750f05ad57a836269b046'], ['src/effects/adapters/fractal.js', '0c90d859a589d4bfd0f9a82b2f601675b6116671e20b2dfba9bab2b98fc72a29'], ['src/effects/adapters/index.js', '40c690ff6ef58619006d0819c5f0f4d419cdfd59a08db55e2276aa9f61430267'], ['src/effects/adapters/julia.js', '0f9cc65f966a358bc4671399e8de49d144d0272a07ef2ae15a0bfb57048eadd5'], ['src/effects/adapters/median.js', 'e82f18d820533993f74c3436addd8bb271a3ef0db8a53c6771ba4eb1e90b0583'], ['src/effects/adapters/palette.js', '8b7c83ea52c3be218866570517335141f9203905115fc90d2e69b1d8cba54452'], ['src/effects/adapters/snow.js', '202e0dbf9b1b8e0e7278c87527d6e2b740eb0a23385115c4805a389caab96366'],
  ['src/effects/catalog.js', 'd8cf312294ccd915892a4a668432ca2533ab255fb24664d89dee8456331e4ea4'], ['src/effects/definition.js', 'fdade0a1f2ab0773b08b9778807d9901583a540c409a9a275cf2fc1c67f6af02'], ['src/effects/generated/canonical-adapter-data.js', 'ca0b139d776f9433b72534f58df9ff182ec55369e85ce37d422990dc0184baab'], ['src/effects/generated/canonical-kernels.js', '66adc01c7df07298b40eaf74fddb7226fdf87bb18dea75b527640c88d0f40ebe'], ['src/effects/generated/kernels.js', 'b535b989f0f130c44261815d90678deb9996ab3098bb8d1cb5591a8f8d8d3c01'], ['src/effects/generated/upstream-snapshot.js', 'e8f8a421f08b0f5cb495f845a97da321038300b7d0dd41392a60653ce2a82090'], ['src/effects/registry.js', '8b3eac7fd4df8699bf27995987eb534625adbce5fe7aa432649a83f278af9618'], ['src/runtime/pass-runner.js', 'fbfd53470735a07dca317c384b9985bb55383961199815e67aee9adda7e881aa'], ['src/runtime/sampler.js', '1e7dc92a20de983ce8b4afd03f3ea83bc86c010e622c4edc4a0aa702027ed328'], ['src/runtime/surface.js', '0cd69c920a710f636a5208e05b49633fc2747cdc2f5fc61113433ceb9ec8ba59'],
])
const entries = ['src/effects/catalog.js', 'src/effects/generated/upstream-snapshot.js', 'src/csl/glsl-kernel.js', 'src/csl/glsl-runtime.js', 'src/runtime/pass-runner.js', 'src/runtime/surface.js']
const importPatterns = [/\bfrom\s*['"]([^'"\n]+)['"]/g, /^[ \t]*import\s+['"]([^'"\n]+)['"]/gm]
const dynamicPattern = /\bimport\s*\(([^)]*)\)/g
function confine(candidate, why) { const real = fs.realpathSync(candidate); if (!beneath(cpuRoot, real)) throw new Error(`${why}: import escapes immutable snapshot`); if (beneath(liveCpuReal, real)) throw new Error(`${why}: import resolved into live checkout`); return real }
const closure = new Map(); const stack = entries.map((entry) => confine(path.join(cpuRoot, entry), 'entry'))
function enqueueImport(specifier, file) { if (!specifier.startsWith('./') && !specifier.startsWith('../') && !specifier.startsWith('node:')) throw new Error(`bare module specifier ${specifier}`); if (!specifier.startsWith('node:')) stack.push(confine(path.resolve(path.dirname(file), specifier), path.relative(cpuRoot, file))) }
while (stack.length) { const file = stack.pop(); if (closure.has(file)) continue; const text = fs.readFileSync(file, 'utf8'); dynamicPattern.lastIndex = 0; let dynamic = dynamicPattern.exec(text); while (dynamic) { const literal = dynamic[1].trim(); if (!/^(?:"(?:[^"\\]|\\.)*"|'(?:[^'\\]|\\.)*')$/.test(literal)) throw new Error(`nonliteral dynamic import in ${path.relative(cpuRoot, file)}`); enqueueImport(literal.slice(1, -1), file); dynamic = dynamicPattern.exec(text) } closure.set(file, sha256(Buffer.from(text))); for (const pattern of importPatterns) { pattern.lastIndex = 0; let match = pattern.exec(text); while (match) { enqueueImport(match[1], file); match = pattern.exec(text) } } }
const closureRecords = [...closure.entries()].map(([file, digest]) => ({ relative_path: path.relative(cpuRoot, file), sha256: digest })).sort((a, b) => a.relative_path.localeCompare(b.relative_path))
if (JSON.stringify(closureRecords.map(({ relative_path, sha256: digest }) => [relative_path, digest])) !== JSON.stringify(expectedClosure)) throw new Error(`CPU import closure mismatch: expected ${expectedClosure.length}, found ${closureRecords.length}`)

const load = (relative) => import(pathToFileURL(confine(path.join(cpuRoot, relative), 'load')).href)
const { canonicalAdapterFactories, canonicalKernelFactories, kernelFactories } = await load('src/effects/catalog.js')
const { UPSTREAM_REVISION } = await load('src/effects/generated/upstream-snapshot.js')
const { createCanonicalBindings } = await load('src/csl/glsl-kernel.js')
const { bindGlslKernel } = await load('src/csl/glsl-runtime.js')
const { runPass } = await load('src/runtime/pass-runner.js')
const { Surface } = await load('src/runtime/surface.js')
if (process.version !== authorityNode || UPSTREAM_REVISION !== upstreamRevisionExpected) throw new Error('LightLeak authority drift')
const canonicalFactory = canonicalKernelFactories[programKey]; const publicFactory = kernelFactories.get(programKey); const canonicalText = Function.prototype.toString.call(canonicalFactory)
if (typeof canonicalFactory !== 'function' || canonicalFactory.name !== factoryName || Buffer.byteLength(canonicalText) !== factoryBytesExpected || sha256(canonicalText) !== factoryShaExpected) throw new Error('canonical LightLeak factory drift')
if (publicFactory !== canonicalFactory) throw new Error('public LightLeak factory is not canonical identity')
if (Object.prototype.hasOwnProperty.call(canonicalAdapterFactories, programKey)) throw new Error('LightLeak adapter override present')
const sourceBytes = fs.readFileSync(sourcePath); if (sourceBytes.length !== sourceBytesExpected || sha256(sourceBytes) !== sourceShaExpected) throw new Error('LightLeak source provenance drift')

const bindingAbi = { inputTex: 'sampler2D', resolution: 'Vec2', tileOffset: 'Vec2', fullResolution: 'Vec2', alpha: 'number', color: 'Vec3', speed: 'number', seed: 'int32', time: 'number' }
const inputFixtureSchema = 'noisemaker-for-cpp.lightleak192.input-texture.v1'
const cases = [
  { name: 'alpha-zero-corner', width: 1, height: 1, alpha: 0, color: [1, .8, .3], speed: .5, seed: 1, time: 0, tileOffset: [0, 0], fullResolution: [1, 1], phase: 1 }, { name: 'default-nonsquare', width: 7, height: 5, alpha: 1, color: [1, .8, .3], speed: .5, seed: 1, time: 0, tileOffset: [0, 0], fullResolution: [7, 5], phase: 2 }, { name: 'alpha-half', width: 8, height: 6, alpha: .5, color: [.2, .7, 1], speed: .5, seed: 7, time: .125, tileOffset: [0, 0], fullResolution: [8, 6], phase: 3 }, { name: 'alpha-clamped-high', width: 5, height: 5, alpha: 2, color: [1, 0, 0], speed: 1.75, seed: 17, time: .75, tileOffset: [0, 0], fullResolution: [5, 5], phase: 4 }, { name: 'speed-zero', width: 6, height: 4, alpha: .8, color: [.8, .2, .1], speed: 0, seed: 3, time: 1.25, tileOffset: [0, 0], fullResolution: [6, 4], phase: 5 }, { name: 'speed-high', width: 9, height: 7, alpha: .9, color: [.1, .9, .3], speed: 5, seed: 99, time: .33, tileOffset: [0, 0], fullResolution: [9, 7], phase: 6 }, { name: 'seed-axis', width: 7, height: 6, alpha: .65, color: [.4, .5, .9], speed: 1.2, seed: 64, time: .625, tileOffset: [0, 0], fullResolution: [7, 6], phase: 7 }, { name: 'time-negative', width: 8, height: 5, alpha: .7, color: [1, .1, .6], speed: 2.25, seed: 12, time: -.4, tileOffset: [0, 0], fullResolution: [8, 5], phase: 8 }, { name: 'tiled-full-resolution', width: 6, height: 4, alpha: .75, color: [.9, .8, .1], speed: .75, seed: 23, time: .44, tileOffset: [7, 11], fullResolution: [19, 17], phase: 9 }, { name: 'color-blue', width: 4, height: 8, alpha: .55, color: [0, .1, 1], speed: 1.5, seed: 5, time: .91, tileOffset: [0, 0], fullResolution: [4, 8], phase: 10 }, { name: 'fullresolution-fallback', width: 5, height: 3, alpha: .85, color: [.6, .6, .6], speed: 1, seed: 8, time: .2, tileOffset: [1, 2], fullResolution: [0, 0], phase: 11 },
]
function inputSurface(definition) { const data = new Float32Array(definition.width * definition.height * 4); for (let i = 0; i < data.length; i += 4) { const p = i / 4; const x = p % definition.width; const y = Math.floor(p / definition.width); data[i] = f(((x * 17 + y * 11 + definition.phase) % 23) / 22); data[i + 1] = f(((x * 7 + y * 19 + definition.phase * 2) % 29) / 28); data[i + 2] = f(((x * 13 + y * 5 + definition.phase * 3) % 31) / 30); data[i + 3] = f(.4 + ((x + y + definition.phase) % 5) / 10) } return new Surface(definition.width, definition.height, data) }
const inputFixtureContract = { schema: inputFixtureSchema, source_function: 'inputSurface', source_function_sha256: sha256(Buffer.from(Function.prototype.toString.call(inputSurface))), coordinate_order: 'x-fastest row-major', component_order: ['r', 'g', 'b', 'a'], formulas: ['f(((x * 17 + y * 11 + phase) % 23) / 22)', 'f(((x * 7 + y * 19 + phase * 2) % 29) / 28)', 'f(((x * 13 + y * 5 + phase * 3) % 31) / 30)', 'f(.4 + ((x + y + phase) % 5) / 10)'] }
function inputFixture(definition) { const surface = inputSurface(definition); return { phase: definition.phase, f32_words_le: Array.from(words(surface.data), u32Hex), f32_sha256: sha256(bytes(surface.data)), rgba8_bytes: Array.from(surface.toRgba8()), rgba8_sha256: sha256(bytes(surface.toRgba8())) } }
function render(factory, definition) { const inputTex = inputSurface(definition); const before = words(inputTex.data).slice(); const output = new Surface(definition.width, definition.height); const bindings = createCanonicalBindings({ width: definition.width, height: definition.height, time: f(definition.time), frame: 0, seed: definition.seed, uniforms: { alpha: f(definition.alpha), color: new Float32Array(definition.color.map(f)), speed: f(definition.speed) }, textures: { inputTex }, tileOffset: new Float32Array(definition.tileOffset.map(f)), fullResolution: new Float32Array(definition.fullResolution.map(f)) }); runPass({ kernel: bindGlslKernel(factory, bindings), destination: output, time: definition.time, seed: definition.seed }); if (before.some((word, index) => word !== words(inputTex.data)[index])) throw new Error(`${definition.name}: input mutated`); if (output.data.some((value) => !Number.isFinite(value))) throw new Error(`${definition.name}: non-finite output`); return output }
function compare(reference, candidate) { const left = words(reference.data); const right = words(candidate.data); const dimensionsMatch = reference.width === candidate.width && reference.height === candidate.height; const laneCountMatch = left.length === right.length; let mismatch = Math.abs(left.length - right.length); let first = null; const common = Math.min(left.length, right.length); for (let i = 0; i < common; i += 1) if (left[i] !== right[i]) { mismatch += 1; if (!first) first = { lane_index: i, top_down_xy: [Math.floor(i / 4) % reference.width, Math.floor(i / 4 / reference.width)], channel: ['r', 'g', 'b', 'a'][i % 4], reference_bits_le: u32Hex(left[i]), candidate_bits_le: u32Hex(right[i]) } } if (!first && left.length !== right.length) first = { lane_index: common, top_down_xy: null, channel: null, reference_bits_le: left[common] === undefined ? null : u32Hex(left[common]), candidate_bits_le: right[common] === undefined ? null : u32Hex(right[common]) }; const a = Uint8Array.from(reference.toRgba8()); const b = Uint8Array.from(candidate.toRgba8()); const rgbaCountMatch = a.length === b.length; let rgbaMismatch = Math.abs(a.length - b.length); let firstRgba = null; const commonBytes = Math.min(a.length, b.length); for (let i = 0; i < commonBytes; i += 1) if (a[i] !== b[i]) { rgbaMismatch += 1; if (!firstRgba) firstRgba = { byte_index: i, top_down_xy: [Math.floor(i / 4) % reference.width, Math.floor(i / 4 / reference.width)], channel: ['r', 'g', 'b', 'a'][i % 4], reference_byte: a[i], candidate_byte: b[i] } } if (!firstRgba && a.length !== b.length) firstRgba = { byte_index: commonBytes, top_down_xy: null, channel: null, reference_byte: a[commonBytes] ?? null, candidate_byte: b[commonBytes] ?? null }; return { exact: dimensionsMatch && laneCountMatch && mismatch === 0 && rgbaCountMatch && rgbaMismatch === 0, dimensions_match: dimensionsMatch, reference_lane_count: left.length, candidate_lane_count: right.length, lane_count_match: laneCountMatch, exact_f32_bits: dimensionsMatch && laneCountMatch && mismatch === 0, reference_rgba8_byte_count: a.length, candidate_rgba8_byte_count: b.length, rgba8_count_match: rgbaCountMatch, exact_rgba8_bytes: rgbaCountMatch && rgbaMismatch === 0, mismatched_lanes: mismatch, mismatched_bytes: rgbaMismatch, first_mismatch: first, first_rgba8_mismatch: firstRgba } }
function selfSurface(width, height, values, rgba = [0, 0, 0, 255]) { return { width, height, data: new Float32Array(values), toRgba8: () => Uint8Array.from(rgba) } }
function selfTests() { const good = compare(selfSurface(1, 1, [0, 1, 0, 1]), selfSurface(1, 1, [0, 1, 0, 1])); const dimensions = compare(selfSurface(1, 1, [0, 1, 0, 1]), selfSurface(2, 1, [0, 1, 0, 1])); const short = compare(selfSurface(1, 1, [0, 1, 0, 1]), selfSurface(1, 1, [0, 1, 0])); const long = compare(selfSurface(1, 1, [0, 1, 0, 1]), selfSurface(1, 1, [0, 1, 0, 1, 0])); const rgba = compare(selfSurface(1, 1, [0, 1, 0, 1]), selfSurface(1, 1, [0, 1, 0, 1], [0, 0, 0, 254])); const signedZero = compare(selfSurface(1, 1, [0, -0, 0, 1]), selfSurface(1, 1, [0, 0, 0, 1])); const n1 = selfSurface(1, 1, [NaN, 0, 0, 1]); const n2 = selfSurface(1, 1, [NaN, 0, 0, 1]); new Uint32Array(n1.data.buffer)[0] = 0x7fc00001; new Uint32Array(n2.data.buffer)[0] = 0x7fc00002; const nan = compare(n1, n2); const result = { good_equal: good.exact, dimensions_mismatch: !dimensions.exact && !dimensions.dimensions_match, short_lane_count: !short.exact && !short.lane_count_match && short.reference_lane_count === 4 && short.candidate_lane_count === 3, long_lane_count: !long.exact && !long.lane_count_match && long.reference_lane_count === 4 && long.candidate_lane_count === 5, rgba8_mismatch: !rgba.exact && !rgba.exact_f32_bits && false || (!rgba.exact && rgba.exact_f32_bits && !rgba.exact_rgba8_bytes && rgba.mismatched_bytes === 1), signed_zero_rejected: !signedZero.exact && !signedZero.exact_f32_bits, nan_payload_rejected: !nan.exact && !nan.exact_f32_bits }; if (!Object.values(result).every(Boolean)) throw new Error(`comparer self-test failed: ${JSON.stringify(result)}`); return result }
const baseSelfTests = selfTests
selfTests = () => { const base = baseSelfTests(); const rgbaShort = compare(selfSurface(1, 1, [0, 1, 0, 1]), selfSurface(1, 1, [0, 1, 0, 1], [0, 0, 0])); const rgba8_byte_count = !rgbaShort.exact && rgbaShort.exact_f32_bits && !rgbaShort.rgba8_count_match && rgbaShort.reference_rgba8_byte_count === 4 && rgbaShort.candidate_rgba8_byte_count === 3 && rgbaShort.mismatched_bytes === 1; if (!rgba8_byte_count) throw new Error('RGBA8 byte-count comparer self-test failed'); return { ...base, rgba8_byte_count } }
const canonicalResults = new Map(); const renderCases = cases.map((definition) => { const direct = render(canonicalFactory, definition); const repeat = render(canonicalFactory, definition); const publicResult = render(publicFactory, definition); const repeatComparison = compare(direct, repeat); const publicComparison = compare(direct, publicResult); if (!repeatComparison.exact || !publicComparison.exact) throw new Error(`${definition.name}: identity drift`); canonicalResults.set(definition.name, direct); return { name: definition.name, width: definition.width, height: definition.height, controls: { alpha: f(definition.alpha), color: definition.color.map(f), speed: f(definition.speed), seed: definition.seed, time: f(definition.time), resolution: [definition.width, definition.height], tile_offset: definition.tileOffset.map(f), full_resolution: definition.fullResolution.map(f) }, binding_abi: bindingAbi, input_texture: inputFixture(definition), output_f32_words_le: Array.from(words(direct.data), u32Hex), output_rgba8_bytes: Array.from(direct.toRgba8()), output_f32_sha256: sha256(bytes(direct.data)), output_rgba8_sha256: sha256(bytes(direct.toRgba8())), repeat_identity: repeatComparison, public_identity: publicComparison, input_immutable_exact_bits: true } })
function replaceExact(text, from, to, expected, label) { const count = text.split(from).length - 1; if (count !== expected) throw new Error(`${label}: expected ${expected} mutation anchors, found ${count}`); return expected === 1 ? text.replace(from, to) : text.replaceAll(from, to) }
function mutation(name, mutate, witnesses, anchorCount) { const mutatedText = mutate(canonicalText); if (mutatedText === canonicalText) throw new Error(`${name}: mutation made no change`); const factory = Function(`"use strict"; return (${mutatedText});`)(); const results = cases.map((definition) => ({ name: definition.name, comparison: compare(canonicalResults.get(definition.name), render(factory, definition)) })); const required = witnesses.map((witness) => { const result = results.find((item) => item.name === witness); if (!result || result.comparison.exact) throw new Error(`${name}: witness ${witness} did not diverge`); return { case: witness, mismatched_lanes: result.comparison.mismatched_lanes, first_mismatch: result.comparison.first_mismatch } }); const [sourceAnchorText, replacementText] = MUTATION_ANCHORS[name]; return { name, anchor_count: anchorCount, source_anchor_text: sourceAnchorText, replacement_text: replacementText, source_anchor_sha256: sha256(sourceAnchorText), replacement_sha256: sha256(replacementText), required_witnesses: witnesses, required_witness_results: required, divergent_cases: results.filter((item) => !item.comparison.exact).map((item) => item.name), mutated_factory_sha256: sha256(mutatedText) } }
const MUTATION_ANCHORS = { 'out-cell-color-materialization': ['mix(hash33(s), color, 0.6000000238418579)', 'mix(hash33(s), new $runtime.PooledFloat32Array([1, 1, 1]), 0.6000000238418579)'], 'out-cell-dist-materialization': ['voronoiCell.__out__ = [cell_color, cell_dist];', 'voronoiCell.__out__ = [cell_color, cell_dist + 0.01];'], 'base-bare-call-site': ['seed_f, t, base_cell', 'seed_f + 1, t, base_cell'], 'warp-bare-call-site': ['seed_f, t, warp_cell', 'seed_f + 1, t, warp_cell'], 'source-global-POINT_COUNT': ['var POINT_COUNT = 6;', 'var POINT_COUNT = 5;'], 'loop-bound-POINT_COUNT': ['i < POINT_COUNT', 'i < 5'], 'alpha-control-axis': ['var blend_alpha = clamp(alpha, 0, 1);', 'var blend_alpha = 1;'], 'speed-control-axis': ['var t = time * speed;', 'var t = time;'], 'seed-control-axis': ['var seed_f = (seed);', 'var seed_f = 1;'], 'color-control-axis': ['mix(hash33(s), color, 0.6000000238418579)', 'mix(hash33(s), new $runtime.PooledFloat32Array([color[2], color[1], color[0]]), 0.6000000238418579)'], 'time-control-axis': ['var t = time * speed;', 'var t = -time * speed;'] }
const mutations = [mutation('out-cell-color-materialization', (text) => replaceExact(text, 'mix(hash33(s), color, 0.6000000238418579)', 'mix(hash33(s), new $runtime.PooledFloat32Array([1, 1, 1]), 0.6000000238418579)', 1, 'out cell color'), ['color-blue'], 1), mutation('out-cell-dist-materialization', (text) => replaceExact(text, 'voronoiCell.__out__ = [cell_color, cell_dist];', 'voronoiCell.__out__ = [cell_color, cell_dist + 0.01];', 1, 'out cell distance'), ['default-nonsquare'], 1), mutation('base-bare-call-site', (text) => replaceExact(text, 'seed_f, t, base_cell', 'seed_f + 1, t, base_cell', 1, 'base bare call'), ['default-nonsquare'], 1), mutation('warp-bare-call-site', (text) => replaceExact(text, 'seed_f, t, warp_cell', 'seed_f + 1, t, warp_cell', 1, 'warp bare call'), ['default-nonsquare'], 1), mutation('source-global-POINT_COUNT', (text) => replaceExact(text, 'var POINT_COUNT = 6;', 'var POINT_COUNT = 5;', 1, 'source POINT_COUNT'), ['default-nonsquare'], 1), mutation('loop-bound-POINT_COUNT', (text) => replaceExact(text, 'i < POINT_COUNT', 'i < 5', 1, 'loop POINT_COUNT'), ['speed-high'], 1), mutation('alpha-control-axis', (text) => replaceExact(text, 'var blend_alpha = clamp(alpha, 0, 1);', 'var blend_alpha = 1;', 1, 'alpha control'), ['alpha-half'], 1), mutation('speed-control-axis', (text) => replaceExact(text, 'var t = time * speed;', 'var t = time;', 1, 'speed control'), ['speed-high'], 1), mutation('seed-control-axis', (text) => replaceExact(text, 'var seed_f = (seed);', 'var seed_f = 1;', 1, 'seed control'), ['seed-axis'], 1), mutation('color-control-axis', (text) => replaceExact(text, 'mix(hash33(s), color, 0.6000000238418579)', 'mix(hash33(s), new $runtime.PooledFloat32Array([color[2], color[1], color[0]]), 0.6000000238418579)', 1, 'color control'), ['color-blue'], 1), mutation('time-control-axis', (text) => replaceExact(text, 'var t = time * speed;', 'var t = -time * speed;', 1, 'time control'), ['time-negative'], 1)]
function rejectAbsolute(value, label = 'fixture') { if (typeof value === 'string') { if (/^(?:[A-Za-z]:[\\/]|\\\\|\/)/.test(value) || /(?:^|[\\/])(?:Users|private|tmp|home)[\\/]/.test(value)) throw new Error(`${label}: absolute path serialized`) } else if (Array.isArray(value)) value.forEach((entry, index) => rejectAbsolute(entry, `${label}[${index}]`)); else if (value && typeof value === 'object') Object.entries(value).forEach(([key, entry]) => rejectAbsolute(entry, `${label}.${key}`)) }
const structuralOnly = [{ name: 'unused-resolution-axis', pixel_expectation: 'structurally authenticated binding retained; no pixel witness claimed' }, { name: 'globalCoord-dead-value', pixel_expectation: 'structurally authenticated source mutation; no pixel witness claimed' }]
const fixture = { schema: 'noisemaker-for-cpp.lightleak192.pixel-parity.v1', program_key: programKey, corpus_revision: 'a024dc3a960cc44af454abc7aebce50456c194e6', upstream_revision: UPSTREAM_REVISION, oracle_authority: 'canonicalFactory77 from immutable noisemaker-for-cpu snapshot; no C++ output participates', input_fixture: inputFixtureContract, provenance: { authority_commit: authorityCommit, authority_checkout_clean: true, node_version: process.version, factory: { name: factoryName, bytes: factoryBytesExpected, sha256: factoryShaExpected }, source: { relative_path_from_noisemaker_for_cpp: sourceRelative, bytes: sourceBytesExpected, sha256: sourceShaExpected }, import_closure: closureRecords, binding_abi: bindingAbi, canonical_public_identity: true, adapter_override_absent: true, point_count: 6 }, comparer_self_tests: selfTests(), render_cases: renderCases, mutation_contract: { behavioral_names: mutations.map((item) => item.name), witnesses: Object.fromEntries(mutations.map((item) => [item.name, item.required_witnesses])), structural_names: structuralOnly.map((item) => item.name) }, behavioral_mutation_ledger: mutations, structural_only_mutation_ledger: structuralOnly }
rejectAbsolute(fixture)
const report = `# LightLeak192 exact-parity oracle\n\nCanonical \`${programKey}\` from corpus \`${fixture.corpus_revision}\`; factory \`${factoryName}\`.\n\nThe oracle stores complete raw little-endian Float32 words and independently captured RGBA8 bytes for ${cases.length} deterministic cases. Each case also stores a source-bound input-texture phase, complete input Float32 words/hash, and complete input RGBA8 bytes/hash; native consumers must use those frozen payloads and never infer phase from case order or reimplement the generator formula. Dimensions, lane counts, signed zero, NaN payloads, repeat identity, input immutability, canonical/public identity, and binding tables are checked exactly.\n\n## Input texture contract\n\nSchema: \`${inputFixtureContract.schema}\`; source function: \`${inputFixtureContract.source_function}\`; source-function SHA-256: \`${inputFixtureContract.source_function_sha256}\`; coordinate order: ${inputFixtureContract.coordinate_order}; component order: ${inputFixtureContract.component_order.join(', ')}.\n\n## Regeneration\n\n\`\`\`sh\nnode docs/port-engineering/counted-for-parity/lightleak192-oracle/lightleak192_oracle_generator.mjs --write --cpu-root \"$NOISEMAKER_CPU_ROOT\"\nnode docs/port-engineering/counted-for-parity/lightleak192-oracle/lightleak192_oracle_generator.mjs --check --cpu-root \"$NOISEMAKER_CPU_ROOT\"\npython3 -B tools/glslcpp/generate_lightleak192_native_oracle_include.py --write\npython3 -B tools/glslcpp/generate_lightleak192_native_oracle_include.py --check\n\`\`\`\n\nBehavioral mutations: ${mutations.length}; structural-only mutations: 2; frozen CPU import closure files: ${closureRecords.length}.\n`
function writeAsset(target, payload) { fs.writeFileSync(target, payload); fs.writeFileSync(sidecarPath(target), sidecarText(target, payload)) }
if (mode !== '--write') { verifySidecar(generatorPath); verifySidecar(outputPath); verifySidecar(reportPath); if (mode === '--check') { if (fs.readFileSync(outputPath, 'utf8') !== `${JSON.stringify(fixture, null, 2)}\n`) throw new Error('LightLeak JSON drift'); if (fs.readFileSync(reportPath, 'utf8') !== report) throw new Error('LightLeak report drift') } } else { writeAsset(outputPath, Buffer.from(`${JSON.stringify(fixture, null, 2)}\n`)); writeAsset(reportPath, Buffer.from(report)); fs.writeFileSync(sidecarPath(generatorPath), sidecarText(generatorPath, fs.readFileSync(generatorPath))) }
console.log(`LightLeak192 oracle ${mode.slice(2)}: ${cases.length} cases, ${mutations.length} behavioral mutations, 2 structural-only mutations`)
