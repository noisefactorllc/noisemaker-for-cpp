# Task 29 Focus Blur borrowed sampler implementation plan

## Scope

Implement only `mixer/focusBlur:focusBlur` / profile
`focus-blur-borrowed-sampler-parameters-v1` from frozen brief SHA sidecar. No
parser, IR, runtime, CMake, corpus, adapter, generic sampler type, other program,
or Git change.

Owned files: a new exact profile module; generator; emitter; slice spec; Python
and native generated-kernel tests; generated typed C++, manifest, and catalog.

## TDD execution

1. Preflight all Task29 sidecars, recompute/oracle checks, accepted Task28
   hashes, corpus/generator checks, and fresh warnings-as-errors Debug CTest.
2. RED on missing profile. Implement exact source/tree/interface/resource/
   loop/call/use authenticator. Return exact helper, parameters, four recursive
   sampler uses, calls, and parents; apply returns the same program.
3. RED on unknown spec field. Add only optional
   `focus_blur_borrowed_sampler_profile`, exact one-row census, disjointness
   from all prior carriers, sorted row, projected counts/order/hashes, and
   manifest field.
4. RED on validator keyword. Independently authenticate exact Focus Blur,
   require the carrier, traverse unchanged, record exact parameter/use/call/
   parent visits, and reject foreign/absent/mutated/coexistent carriers.
5. RED on emitter keyword/current sampler type error. Independently
   authenticate. In `function_parameter_type` return `const Surface&` only for
   the two exact authenticated sites. Keep generic type spelling fail closed.
   Count exact declaration/definition parameters, calls, arguments, and uses;
   require exact completion.
6. Build exhaustive one-axis mutations with candidate/precondition key-set
   equality and protected-coordinate equality. Cover full source, interface,
   resource, helper, parameter, recursive use, complete call ancestry and exact
   predicate/if/then/else objects plus slot ownership, loop,
   numeric/define/hash/carrier/coexistence/foreign/equal-tree matrices. Every
   candidate rejects independently at all three boundaries.
7. Regenerate canonically. Reconstruct actual Task28 by removing only Focus
   Blur from the live Task29 spec and running the real pipeline. Require exact
   Task28 artifacts: typed C++ `b53e020b...`, manifest `612d3522...`, catalog
   `372d1f69...`; compare all 128 historical blocks/rows and 130 public
   mappings, normalizing only namespace ordinal/common monolithic hash.
8. Transcribe six public cases and eight direct modes. Each executes its own
   no-default switch arm with execution-derived ABI/resource/alias/ownership/
   null/write/read witnesses; by-value owns and reads two real Surface copies.
   Semantic signatures exclude IDs/names/acceptance/results and are pairwise
   unique. Test all bindings and
   lifetime-after-Bindings destruction. Implement distinct exhaustive branch,
   sampler-role, 63/64/65 sample, and alpha-read mutations. Authenticate all
   executable table/switch/witness fields, exact 67 sample and 67 size-query
   counters, declared/handled/observed equality, switch/witness tamper, invalid
   enum rejection, and predicate/branch/call move-copy-swap controls.
9. Assert generated shape: two `const Surface&` sites in helper declaration
   and two in definition only; exact two dereferenced-state call orders; state
   stays `const Surface*`; no mutable/pointer/value/wrapper/copy/retention/null/
   dynamic lookup/generic sampler forms.
10. Run Task29 oracle, corpus/generator, focused and full Python, all Task15-29
    oracles, fresh Debug/Release CTest, ASan/UBSan (Apple leak retry only if
    needed), stack usage, and release AArch64 disassembly. Prove direct
    reference ABI, fixed stack, one selected direct call, 67 reads plus 67
    size queries, no heap/exception/indirect/dynamic stack.
11. Record final hashes/evidence, dispatch independent implementation review,
    fix every Critical/Important under TDD, and re-review before acceptance.

## Stop conditions

Stop on any frozen identity/oracle/Task28 drift, any generic sampler widening,
any unstable/owning/mutable borrow, vacuous mutation arm, unexpected historical
generated delta, sanitizer issue, or unresolved Critical/Important finding.
