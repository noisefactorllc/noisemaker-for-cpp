# Task 25 Task 2 independent review

Date: 2026-08-11

## Verdict

**APPROVED.** The final Task 2 bytes satisfy the amended brief and approved
implementation design sections 5-7. No blocking or non-blocking finding
remains in the reviewed scope.

## Binding and reviewed hashes

| Artifact or file | SHA-256 |
| --- | --- |
| amended Task 25 brief | `193523a6e94642c7c1b6c4da86b08de8616d0d15a3edc0b3257e068ac930ddd2` |
| approved implementation design | `9900749a387a30c6f1db0e584d2382288d44c2f3b0d5670f3b1968f1312c357b` |
| `tools/glslcpp/frontend/literal_vec3_lane_index_profile.py` | `62920c523ab4a73e4a6c75fe912459bdf7ccde86196871eda2eeab16c69ca216` |
| `tools/glslcpp/generate_typed_slice.py` | `8c453c5526dee235232df1d25ada88294d120e2023632fbfa89da1b425ff7df1` |
| `tools/glslcpp/emit_typed_cpp.py` | `18659e9b2e76e14d2da69ce198b21ce653d325525a67b290b856ee7af61dbd1c` |
| `tests/test_typed_generator.py` | `0e89165df7f7c40652075e6a83d68e3aee7ffa3a4a89844c49fd6e57aeb99bc2` |
| Task 2 report | `77caf4936a6a7180f586acba37321f4b05b989f09010d9cbf234260efcf0a343` |

The final helper change is the private selected-source provenance detector
used by validator and emitter. It adds no public profile API, typed-IR field,
manifest field, capability, compatibility transform, or runtime route.

## Review results

- The future loader form requires the exact sorted 125-key digest
  `9b8f94754fc40e8f8701bb539d403d44343dbfc9c351809a6ca6dbbd468cdbd4`,
  exactly the two selected `{}` carrier records, and exact global capability,
  numeric, compatibility, and define maps. Missing, wrong, duplicate,
  reordered, substituted, additional, foreign/Grade-LUT, compatibility, and
  numeric/capability vocabulary variants reject.
- The live Task 24 form remains exactly 123 sorted unique programs with digest
  `df7750a48a77733e50f0e20072e291c2404e761932f6df8ddbcf8793d58fb6ac`
  and no lane carriers. This is a narrowly pinned transition only. **Task 3
  must remove this 123-program branch when it installs the 125-entry slice.**
- The driver performs analyze/profile application, requires a distinct post,
  authenticates the transition, and sends the post plus carrier independently
  to validator and emitter. Both selected keys reject forged analyzer pre,
  already-post analyzer output, and forged application post; every driver
  negative preserves all three repository generated byte snapshots.
- Validator and emitter independently authenticate exact post structure and
  metadata. Exact pre/post controls implement all four modes, including
  dataclass-equal post reconstruction acceptance only with the exact carrier.
  The private provenance guard closes foreign-key combinations while either
  frozen raw or normalized source identity remains.
- The literal Cartesian is present at the required boundaries: every forged
  pre is exercised across 3 carrier modes by 4 caller-hash modes at
  application; every forged post is exercised across the same literal matrix
  at validator and emitter independently. Exact controls use the same matrix.
- Exact mutations cover all 11 frozen paths, 8/3 per-key inventories, zero,
  partial, additional, shared-identity duplicate, move, reorder,
  already/twice-transformed states, site fields, lane/member, base identity and
  origin, index forms, read/write conversion, compound/prefix/postfix context,
  RHS order, and the Lens line-260 nonsplat case. Each fixture asserts its real
  path, census, identity, AST context, or language-boundary precondition.
- Real parsed/analyzed fixtures cover alternate local, parameter/global/
  uniform and other base types; uniform/induction/effectful/nested/runtime
  indexes; alias/delayed/out/inout escape; globals, arrays, matrices, structs,
  UBOs, loops, derivatives, sampler/fetch/output/function/early-return/
  recursion insertions; and actual parser/semantic rejection for invalid
  indexes, allocation, callback, exception, indirect call, pointer-like syntax,
  and dynamic stack. Nearby authentic controls reach their intended boundary.
- Corpus preflight mutations cover revision, effect/program/program-key/
  runtime-key, source path and raw/normalized identity, status/adapter, pass,
  output, and varying drift for both selected entries.
- Emission is the existing fixed route only: six
  `glsl::set_swizzle<I>(hsv, rhs)` writes, five
  `glsl::swizzle<I>(hsv)` reads, zero selected `operator[]`, and the line-260
  scalar-to-vec3 splat. Read/write role inversion rejects. Ordinary and
  `object.__new__` emitters retain an empty authorization tuple.
- Mechanical search found no lane carrier/profile or generic index widening in
  `typed_ir.py`, the live slice, generated manifest, or generated catalog.

## Fresh independent verification

```text
Task 25 Task 1+2 focused methods: Ran 10 tests in 189.510s; OK
python3 -m py_compile (four owned Python files): exit 0
python3 tools/glslcpp/generate_typed_slice.py --check:
  generate_typed_slice: typed slice ok (123 programs); exit 0
live slice: 123 programs; sorted_unique=True;
  typed-list SHA-256=df7750a48a77733e50f0e20072e291c2404e761932f6df8ddbcf8793d58fb6ac;
  lane carriers=[]
targeted provenance probe: 4 exact validator/emitter controls accepted;
  12 foreign-key plus raw/normalized/member combinations rejected
```

The implementation report also records a scoped mutation-sensitivity RED of
180 validator failures when the provenance arm was temporarily removed,
followed by GREEN after restoration. Final hashes above prove no temporary
production mutation remains.

No repository file was edited and no Git operation was used during this
independent review.
